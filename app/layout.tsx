import type { Metadata } from "next";
import { Inter, Alfa_Slab_One, Concert_One } from "next/font/google";
import "./globals.css";
  
import { cn } from "@/lib/utils"
import Header from "@/components/layout/header";
import AppCartProvider from "@/components/shop/app-cart-privider";
import { Toaster } from "@/components/ui/toaster"
import Footer from "@/components/layout/footer";
import ProductHighlight from "@/components/layout/highlight";
import Products from "./products/all/page";
import { ProductListHighlight } from "@/components/shop/product-list";
import Home from "./page";
  
 const cormot = Concert_One({weight:[ '400'],subsets:['latin']})
 export const metadata: Metadata = {
  title: "Stilo20",
  description: "Generated by create next app",
};

 
 

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={cn(cormot.className, "min-h-screen flex flex-col")}>
        <AppCartProvider>
          <Header />
          <Toaster />
          <main className={"flex-grow transition duration-3000 ease-in-out"}>
            {children}     
          </main>
          <Footer />
        </AppCartProvider>
      </body>
    </html>
  );
}
